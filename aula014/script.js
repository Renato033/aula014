/* let nome = "Rafael"
alert(nome.toUpperCase())
alert(nome.toUpperCase()) */

/* let dinheiro = 5000
alert(dinheiro.toLocaleString(`pt-BR`, {
  style: `currency`,
  currency: `USD`,

})) */
let fruta = "Uva"
alert(fruta.length)